### Timeline UI

Use `components/reui/timeline.tsx` for all timeline, activity log, audit log, history, and status-history UI.

Do not create plain custom timeline components from scratch. Feature-specific timeline components may format data, but the base timeline UI must use the REUI Timeline component.
